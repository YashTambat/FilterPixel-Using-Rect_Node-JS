{
  "compilerOptions": {
    "target": "es6",                                  /* Set the JavaScript language version for emitted JavaScript. */
    "module": "commonjs",                             /* Specify what module code is generated (Node.js uses CommonJS). */
    "esModuleInterop": true,                          /* Emit additional JavaScript for importing CommonJS modules. */
    "strict": true,                                   /* Enable all strict type-checking options. */
    "skipLibCheck": true,                             /* Skip type checking for declaration files. */
    "outDir": "./dist",                               /* Specify the output folder for compiled JavaScript. */
    "rootDir": "./src",                               /* Specify the root folder for TypeScript files. */
    "sourceMap": true,                                /* Enable source maps for easier debugging. */
    "resolveJsonModule": true,                        /* Enable importing JSON modules. */
    "lib": ["es6", "dom"],                            /* Specify the libraries available in the environment. */
    "forceConsistentCasingInFileNames": true          /* Ensure consistent casing in imports. */
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules"]
}
